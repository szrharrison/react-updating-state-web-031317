describe('Tests', function () {

  it(`shouldn't be a part of this readme lesson.`, function () {
    expect(true).toEqual(true);
  });

  it('should exist since this lesson expects there to be tests for some reason.', function () {
    expect(true).toEqual(true);
  });
});
